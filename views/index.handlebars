<h1>Eat da burger!</h1>


{{!-- <ul>
    {{#each burgers_name}}
    {{#unless devoured}}
    {{> burgers/burger-block sleep=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers devoured!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block sleep=false}}
    {{/if}}
    {{/each}}
</ul> --}}

<ul>
    {{#each burgers}}
    <li>
        <p>
            {{this.id}}. {{this.burger_name}}

            <button data-burgerid="{{this.id}}" class="devour">Devour!</button>
        </p>
    </li>
    {{/each}}
</ul>






<h2>Enter a burger you want to eat!</h2>
<form class="create-form">

    <div class="form-group">
        <input type="text" id="burger-to-order" name="name">
    </div>

    <button id="submit" type="submit">Submit</button>
</form>